#!/bin/bash
# ============================================
#   XLLucky 金融工具箱 - 双击运行
# ============================================
# 双击此文件即可打开图形化菜单

cd "$(dirname "$0")"

# 激活虚拟环境
if [ -d "venv" ]; then
    source venv/bin/activate
fi

# 直接调用 run.sh 的交互式菜单
./run.sh
